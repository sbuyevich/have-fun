# Roles and workflow changes

- Replace the existing Game Master/Master role with Host everywhere: specs, UI text, routes/session role naming, and code models where practical.
- When the app is opened with no query parameters, assign the current browser session the Host role automatically.
- Players join through the register page.
- The configured master-name entry flow is removed for this v1.0 workflow.

# Landing page for host role

- Landing page for host is Home page.
- Home page should show URL and QR with Register page route.
- Player can load Register page using it